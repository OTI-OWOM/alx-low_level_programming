#include "main.h"
/**
 * _isupper - check if character is upper
 *@c: The number to be checked
 * 
 *Return: 1 for upper character or 0 otherwise
 */

int _isupper(int x)
{

if ( x >= 65 && x <=90)
{
return (1);
}
return (0);
}
